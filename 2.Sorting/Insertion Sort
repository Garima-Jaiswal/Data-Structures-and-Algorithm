  public static void insertionSort(int n[]){
        for(int i=1;i<n.length;i++){
            int curr= n[i];
            int prev= i-1;
            while(prev>=0 && (n[prev]> curr)){
                n[prev+1] =n[prev];
                prev--;
            }
            n[prev+1]= curr;
        }
    } 
